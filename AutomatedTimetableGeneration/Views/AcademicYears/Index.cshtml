@model IEnumerable<AutomatedTimetableGeneration.Models.AcademicYear>

@{
    ViewBag.Title = "Index";
}


<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr class="success">
        <th>
            @Html.DisplayNameFor(model => model.StudentsCount)
        </th>
        <th>
            @Html.DisplayName("Group Count")
        </th>
        <th>
            @Html.DisplayName("Section Count")
        </th>
        <th>
            @Html.DisplayName("Academic Year")
        </th>
        <th>
            @Html.DisplayName("Day Work Hours")
        </th>
        <th>
            @Html.DisplayName("Allowed Gap Hours")
        </th>
        <th>
            @Html.DisplayName("Year")
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.StudentsCount)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.GroupCount)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SectionCount)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AcademicYear1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DayWorkHours)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AllowedGapHours)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Year.ID)
        </td>
        <td>
            <span class="btn default btn-xs grey"> @Html.ActionLink("Edit", "Edit", new { id = item.ID }) </span>|
            <span class="btn default btn-xs grey"> @Html.ActionLink("Details", "Details", new { id = item.ID }) </span>|
            <span class="btn default btn-xs yellow-lemon"> @Html.ActionLink("Delete", "Delete", new { id = item.ID })</span>
        </td>
    </tr>
}

</table>
